# Copy this file to .env and fill in your Supabase project details to enable REAL mode.
# When not set, the app runs in MOCK mode without network calls.

REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
# Optional: used for Supabase email redirects and OAuth callback target
REACT_APP_FRONTEND_URL=http://localhost:3000

# Explicitly enable mock mode even if the above are set (for testing)
# REACT_APP_MOCK_MODE=true
