# Create React App environment variables (load at build time)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
# Optional, used for OAuth/email redirect URLs. Falls back to window.location.origin
REACT_APP_FRONTEND_URL=
# Force mock mode regardless of env (optional). When true, Supabase calls are mocked.
# REACT_APP_MOCK_MODE=true
