# Create React App environment variables for Supabase (browser-safe)
# Copy this file to .env and set values from your Supabase project.
# Note: Only use the "anon" public key in frontend apps.

# Your Supabase project URL (e.g., https://xyzcompany.supabase.co)
REACT_APP_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase "anon" public key (Project Settings -> API -> Project API keys)
REACT_APP_SUPABASE_ANON_KEY=your-anon-public-key

# Optional: Frontend site URL used for email redirect links during auth
# Defaults to window.location.origin if not set
# REACT_APP_FRONTEND_URL=http://localhost:3000
